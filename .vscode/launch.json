{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Population ARIMA",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app.main:app", "--reload", "--port", "8000"],
      "jinja": true,
      "justMyCode": true,
      "cwd": "${workspaceFolder}/population-arima"
    }
  ]
}
